apiVersion: v1
kind: Pod
metadata:
  name: memory-hog
spec:
  containers:
  - name: hog
    image: polinux/stress
    resources:
      limits:
        memory: "100Mi"
    command:
      - stress
      - --vm
      - "1"
      - --vm-bytes
      - "200M"
      - --vm-hang
      - "1"